/*
 * Forked from ZMK mainline's `corne` shield default keymap, with the
 * outer pinky column dropped on each side (TAB/BSPC and CTRL/' and
 * SHFT/ESC) to fit the 3x5+3 layout. 36 bindings per layer, in the same
 * row-major order as the five_column_transform map in dactyl_cygnus.dtsi.
 *
 * Base layer uses Colemak Mod-DH (matrix variant), not QWERTY.
 */

#include <behaviors.dtsi>
#include <dt-bindings/zmk/keys.h>
#include <dt-bindings/zmk/bt.h>

/ {
        keymap {
                compatible = "zmk,keymap";

                default_layer {
// -----------------------------------------------------------------
// |  Q  |  W  |  F  |  P  |  B  |   |  J  |  L  |  U  |  Y  |  ;   |
// |  A  |  R  |  S  |  T  |  G  |   |  M  |  N  |  E  |  I  |  O   |
// |  Z  |  X  |  C  |  D  |  V  |   |  K  |  H  |  ,  |  .  |  /   |
//             | GUI | LWR | SPC |   | ENT | RSE | ALT |
// TEMPORARY for hardware bring-up: right thumb keys 2 and 3 are normally
// &mo 2 (layer-shift) and &kp RALT (bare modifier) -- neither produces
// visible text in Notepad even when wired correctly, which makes them
// impossible to bench-test by typing. Swapped to visible N8/N9 for testing;
// revert to &mo 2 / &kp RALT once both are confirmed working.
                        bindings = <
   &kp Q &kp W &kp F &kp P &kp B     &kp J &kp L &kp U     &kp Y   &kp SEMI
   &kp A &kp R &kp S &kp T &kp G     &kp M &kp N &kp E     &kp I   &kp O
   &kp Z &kp X &kp C &kp D &kp V     &kp K &kp H &kp COMMA &kp DOT &kp FSLH
               &kp LGUI &mo 1 &kp SPACE   &kp RET &kp N8 &kp N9
                        >;
                };

                lower_layer {
// -----------------------------------------------------------------
// |  1  |  2  |  3  |  4  |  5  |   |  6  |  7  |  8  |  9  |  0   |
// |BTCLR| BT1 | BT2 | BT3 | BT4 |   | LFT | DWN |  UP | RGT |      |
// |     |     |     |     |     |   |     |     |     |     |      |
//             | GUI |     | SPC |   | ENT |     | ALT |
                        bindings = <
   &kp N1     &kp N2       &kp N3       &kp N4       &kp N5         &kp N6   &kp N7   &kp N8 &kp N9    &kp N0
   &bt BT_CLR &bt BT_SEL 0 &bt BT_SEL 1 &bt BT_SEL 2 &bt BT_SEL 3   &kp LEFT &kp DOWN &kp UP &kp RIGHT &trans
   &trans     &trans       &trans       &trans       &trans         &trans   &trans   &trans &trans    &trans
                    &kp LGUI &trans &kp SPACE   &kp RET &trans &kp RALT
                        >;
                };

                raise_layer {
// -----------------------------------------------------------------
// |  !  |  @  |  #  |  $  |  %  |   |  ^  |  &  |  *  |  (  |  )   |
// |     |     |     |     |     |   |  -  |  =  |  [  |  ]  |  \   |
// |     |     |     |     |     |   |  _  |  +  |  {  |  }  |  ~   |
//             | GUI |     | SPC |   | ENT |     | ALT |
                        bindings = <
   &kp EXCL &kp AT &kp HASH &kp DLLR &kp PRCNT   &kp CARET &kp AMPS  &kp ASTRK &kp LPAR &kp RPAR
   &trans   &trans &trans   &trans   &trans      &kp MINUS &kp EQUAL &kp LBKT  &kp RBKT &kp BSLH
   &trans   &trans &trans   &trans   &trans      &kp UNDER &kp PLUS  &kp LBRC  &kp RBRC &kp TILDE
                    &kp LGUI &trans &kp SPACE   &kp RET &trans &kp RALT
                        >;
                };

                navigation_layer {
// Hyprland window/workspace management. Not yet bound to an activation
// key -- pick a key (probably a home-row-mod once that's set up) and
// wire it to &mo 3 (or &lt 3 <key>) once decided.
// -----------------------------------------------------------------
// |SUP+SFT+1|SUP+SFT+2|SUP+SFT+3|SUP+SFT+4|SUP+SFT+5| |SUP+SFT+6|SUP+SFT+7|SUP+SFT+8|SUP+SFT+9|SUP+SFT+0|  move app to workspace
// | SUP+1   | SUP+2   | SUP+3   | SUP+4   | SUP+5   | | SUP+6   | SUP+7   | SUP+8   | SUP+9   | SUP+0   |  switch to workspace
// |         |         |         |         |         | |         |         |         |         |         |  TODO: tmux/browser shortcuts
//                     |  GUI    |  LWR    |  SPC    | |  ENT    |  RSE    |  ALT    |
                        bindings = <
   &kp LG(LS(N1)) &kp LG(LS(N2)) &kp LG(LS(N3)) &kp LG(LS(N4)) &kp LG(LS(N5))   &kp LG(LS(N6)) &kp LG(LS(N7)) &kp LG(LS(N8)) &kp LG(LS(N9)) &kp LG(LS(N0))
   &kp LG(N1)     &kp LG(N2)     &kp LG(N3)     &kp LG(N4)     &kp LG(N5)       &kp LG(N6)     &kp LG(N7)     &kp LG(N8)     &kp LG(N9)     &kp LG(N0)
   &trans         &trans         &trans         &trans         &trans           &trans         &trans         &trans         &trans         &trans
                                  &trans &trans &trans   &trans &trans &trans
                        >;
                };
        };
};
